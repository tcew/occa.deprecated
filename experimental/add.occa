occaKernel void add(occaKernelInfoArg,
                    occaPointer int *a,
                    occaPointer int *b,
                    occaPointer int *c){
  occaOuterFor0{
    occaInnerFor0{
      const int id = occaGlobalId0;
      c[id] = a[id] + b[id];
    }
  }
}
